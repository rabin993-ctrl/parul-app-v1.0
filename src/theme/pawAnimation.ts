/** Shared paw-print loader timing — calm, readable stagger. */
export const PAW_SPLASH_TIMING = {
  fadeInMs: 520,
  fadeOutMs: 460,
  staggerMs: 420,
  holdMs: 800,
  pauseMs: 480,
} as const;

export const PAW_LOADER_TIMING = {
  fadeInMs: 440,
  fadeOutMs: 400,
  staggerMs: 340,
  holdMs: 520,
  pauseMs: 360,
} as const;

export const MIN_SPLASH_MS = 3000;
